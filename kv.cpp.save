#include <stdio.h>
#include <math.h>

main()
{
    float a, b, c, d, x1, x2;

    printf("enter the A value: ");
    scanf("%f", &a);

    printf("enter the B value: ");
    scanf("%f", &b);

    printf("enter the B value: ");
    scanf("%f", &c);
    printf("%f %f %f\n", a, b, c);
    
    d = pow(b, 2) - 4 * a * c;
    
    if (d < 0)
        printf("there is no salvations\n");
    else
    {
        x1 = (b*(-1) + sqrt(d))/(2 * a);
        x2 = (b*(-1) - sqrt(d))/(2 * a)
    }
    printf("%f", d);

}
